from distutils.core import setup
from setuptools import find_packages

setup(
    name="Named Entity Recognition",
    version="0.0.1",
    description="A simple Named Entity Recognition(NER) API",
    packages=find_packages()
)